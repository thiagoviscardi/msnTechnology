import { makeStyles } from '@material-ui/styles';

const estilos = makeStyles({
  root: {
    marginLeft: '3%',
    height: 230,
  },
  header: {
    height: '100',
    width: '100%',
  },
});

export default estilos;
